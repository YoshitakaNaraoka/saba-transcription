# cargo new により配下に作成されると自動的に workspace に追加される
workspace = { members = [ "net/wasabi","saba_core"] }
[package]
name = "saba-transcription"
version = "0.1.0"
edition = "2021"

[dependencies]
noli = { git = "https://github.com/hikalium/wasabi.git", branch = "for_saba" }
